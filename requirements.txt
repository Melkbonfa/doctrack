flask>=3.0.0
flask-cors>=4.0.0
flask-sqlalchemy>=3.1.0
flask-jwt-extended>=4.6.0
flask-bcrypt>=1.0.1
flask-socketio>=5.6.0
pandas>=2.0.0
openpyxl>=3.1.0
python-dotenv>=1.0.0
# Única dependência de rede externa do app: a sincronização da PTAX com o Banco
# Central (custos/cambio.py). O import é tardio e a falha é tolerada — sem ela o
# módulo de Custos segue utilizável com cotação registrada à mão.
requests>=2.31.0
# Servidor WSGI de produção. Era instalado à parte pelo deploy_windows.ps1, fora
# do requirements — o que deixava a versão em produção fora de qualquer controle.
waitress>=3.0.0
gunicorn>=22.0.0
psycopg2-binary>=2.9.9
# Só para scripts/gerar_relatorio_custos.py (gera os PNGs do relatório de custos).
# O app não usa: o PDF do dashboard é montado no navegador com jsPDF.
matplotlib>=3.7.0
jinja2>=3.1.0
